# Version control
.git
.gitignore

# Python
__pycache__
*.py[cod]
*.pyo
.pytest_cache
.venv
venv

# Frontend (built separately, not part of the backend image)
frontend/

# Dev / local config
.env
.env.*
*.local

# Docs and factory (not needed at runtime)
docs/
factory/
context/

# Docker itself
Dockerfile
.dockerignore
docker-compose*.yml
